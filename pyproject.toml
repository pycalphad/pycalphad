[build-system]
requires = [
    "Cython",
    "numpy>=1.13",
    "scipy",
    "setuptools",
    "wheel",
]
# Use the legacy backend because we have a local `versioneer.py` dependency
build-backend = "setuptools.build_meta:__legacy__"
